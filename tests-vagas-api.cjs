const test=require('node:test'),assert=require('node:assert/strict'),fs=require('fs'),vm=require('vm');
test('Domo não consulta nem grava vagas após a transferência',async()=>{let handler;vm.runInNewContext(fs.readFileSync('supabase/functions/domo-vagas/index.ts','utf8').replace(':Request',''),{Deno:{serve:h=>handler=h},Response,JSON});for(const action of['carregar','salvar','importar']){const r=await handler(new Request('https://example.invalid',{method:'POST',body:JSON.stringify({action})}));assert.equal(r.status,410);const body=await r.json();assert.match(body.destino,/diamond/);assert.equal(body.estado,undefined);}});
